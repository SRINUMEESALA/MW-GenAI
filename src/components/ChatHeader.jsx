import React from 'react';
import { Typography } from '@mui/material';

const ChatHeader = () => {
  return (
    <Typography variant="h4" align="center" gutterBottom>
      {/* AI Genome Explorer Chatbot */}
    </Typography>
  );
};

export default ChatHeader;
